<nav id="save-menu" class="navbar navbar-inverse navbar-fixed-bottom" role="navigation">
	<div class="navbar-header">
		<a class="navbar-brand" href="#">Save Needed</a>
        {{#if unsavedRecords}}
            <p class="navbar-text">{{unsavedRecords.length}} change(s) to save</p>
        {{/if}}
	</div>

	<div class="collapse navbar-collapse navbar-ex1-collapse pull-right">
        {{#if revertLabel}}
		    <button type="button" class="btn btn-default navbar-btn" {{action "revert"}}>{{revertLabel}}</button>
        {{else}}
            <button type="button" class="btn btn-default navbar-btn" {{action "revert"}}>Revert</button>
        {{/if}}
		<button type="button" class="btn btn-success navbar-btn" data-toggle="button" data-loading-text="Saving..." id="save-bar-save-button" {{action "save"}}>Save</button>
	</div>	
</nav>